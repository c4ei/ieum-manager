import test from 'node:test';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';
test('audit log write failure does not terminate the caller',()=>{const script=`process.env.IEUM_MANAGER_AUDIT_LOG='/proc/1/ieum-admin-audit.jsonl';const {audit}=await import('./lib/admin-policy.js');const ok=await audit({action:'test'});if(ok!==false)process.exit(2);`;const result=spawnSync(process.execPath,['--input-type=module','--eval',script],{cwd:new URL('..',import.meta.url),encoding:'utf8'});assert.equal(result.status,0,result.stderr);assert.match(result.stderr,/audit log write failed/);});
