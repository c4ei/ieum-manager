const $=id=>document.getElementById(id),publicId=location.pathname.split('/').at(-1);let campaign,captchaToken='';let deviceId=localStorage.getItem('ieum-trial-device');if(!deviceId){deviceId=crypto.randomUUID();localStorage.setItem('ieum-trial-device',deviceId);}
async function load(){const r=await fetch(`/api/trial/${publicId}`),d=await r.json();if(!r.ok)return $('result').textContent=d.error;campaign=d.campaign;$('name').textContent=campaign.name;$('summary').innerHTML=`<h2>${campaign.reward} IEUM 체험 지급</h2><p>남은 캠페인 예산 ${campaign.remaining} IEUM · ${new Date(campaign.endsAt).toLocaleString()}까지</p>`;if(campaign.status==='active'&&new Date(campaign.startsAt)<=new Date()&&new Date(campaign.endsAt)>new Date()&&Number(campaign.remaining)>=Number(campaign.reward)){$('claim').hidden=false;$('claim').querySelector('button').textContent=`${campaign.reward} IEUM 체험하기`;if(campaign.captchaRequired&&d.turnstileSiteKey){const wait=setInterval(()=>{if(window.turnstile){clearInterval(wait);turnstile.render('#captcha-box',{sitekey:d.turnstileSiteKey,callback:t=>captchaToken=t,'expired-callback':()=>captchaToken='' });}},100);}}else $('result').textContent='현재 지급 중인 캠페인이 아닙니다.';}
$('claim').onsubmit=async e=>{e.preventDefault();const button=e.currentTarget.querySelector('button');button.disabled=true;$('result').textContent='안전 조건을 확인하고 지급 대기열에 등록 중입니다…';try{const r=await fetch(`/api/trial/${publicId}/claim`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({address:$('address').value.trim(),deviceId,captchaToken})}),d=await r.json();if(!r.ok)throw new Error(d.error);$('claim').hidden=true;$('result').innerHTML=`지급 완료 · <a href="${d.explorerUrl}">거래 확인</a>`;}catch(x){$('result').textContent=x.message;button.disabled=false;}};load();
