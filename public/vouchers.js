fetch('/api/vouchers/summary').then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.error);document.getElementById('stats').innerHTML=`<h2>상품권 누적 발행액 ${data.issued} IEUM</h2><p>지급 완료 ${data.claimed} IEUM · 미사용 준비액 ${data.outstanding} IEUM</p><p>전체 ${data.count.toLocaleString()}장 · 사용 완료 ${data.claimedCount.toLocaleString()}장</p><small>${data.notice}</small>`;}).catch(error=>document.getElementById('stats').textContent=error.message);
