const $=s=>document.querySelector(s),esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),num=v=>Number(v??0).toLocaleString('ko-KR'),short=v=>v?`${v.slice(0,10)}…${v.slice(-8)}`:'—';let page=1;const size=25;const tick=()=>$('#updated').textContent=new Date().toLocaleString('ko-KR');tick();setInterval(tick,1000);
function paginate(items){const pages=Math.max(1,Math.ceil(items.length/size));page=Math.min(page,pages);$('#detail-pages').innerHTML=`<button ${page<=1?'disabled':''} data-page="${page-1}">이전</button><span>${page} / ${pages}</span><button ${page>=pages?'disabled':''} data-page="${page+1}">다음</button>`;return items.slice((page-1)*size,page*size)}function table(headers,rows){return `<div class="panel table-wrap"><table><thead><tr>${headers.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.join('')}</tbody></table></div>`}
async function entity(){const [kind,raw]=location.pathname.slice(1).split('/');if(!raw)return false;const value=decodeURIComponent(raw),hash=value.startsWith('0x')?value:`0x${value}`;let endpoint;if(kind==='tx')endpoint=`/api/explorer/transaction/${encodeURIComponent(hash)}`;else if(kind==='address')endpoint=`/api/explorer/address/${encodeURIComponent(value)}`;else if(kind==='block')endpoint=/^\d+$/.test(value)?`/api/explorer/block/${value}`:`/api/explorer/block/hash/${encodeURIComponent(hash)}`;else return false;const response=await fetch(endpoint),data=await response.json();if(!response.ok)throw new Error(data.error||`HTTP ${response.status}`);if(kind==='tx'){$('#detail-title').innerHTML='트랜잭션 <span>상세</span>';$('#detail-content').innerHTML=`<article class="panel search-card"><p><b>Hash</b> <code>${esc(data.hash)}</code></p><p><b>Block</b> <a href="/block/${data.block_height}">${num(data.block_height)}</a></p><p><b>From</b> <a href="/address/${esc(data.sender)}">${esc(data.sender)}</a></p><p><b>To</b> <a href="/address/${esc(data.recipient)}">${esc(data.recipient)}</a></p><p><b>Value</b> ${esc(data.value)} wei</p><p><b>Fee</b> ${esc(data.fee)} wei</p></article>`;}else if(kind==='address'){$('#detail-title').innerHTML='주소 <span>상세</span>';const rows=(data.transactions||[]).map(tx=>`<tr><td><a href="/tx/${esc(tx.hash)}">${short(tx.hash)}</a></td><td><a href="/block/${tx.block_height}">${num(tx.block_height)}</a></td><td>${short(tx.sender)} → ${short(tx.recipient)}</td><td>${esc(tx.value)}</td></tr>`);$('#detail-content').innerHTML=`<article class="panel search-card"><p><b>Address</b> <code>${esc(data.account.address)}</code></p><p class="big-balance">${esc(data.account.balance)} wei</p><p>거래 ${num(data.account.tx_count)}건 · 최근 블록 ${num(data.account.last_seen_height)}</p></article>${table(['해시','블록','From → To','Value'],rows)}`;}else{$('#detail-title').innerHTML=`블록 #${num(data.height)} <span>상세</span>`;const rows=(data.transactions||[]).map(tx=>`<tr><td><a href="/tx/${esc(tx.hash)}">${short(tx.hash)}</a></td><td>${short(tx.sender)}</td><td>${short(tx.recipient)}</td><td>${esc(tx.value)}</td></tr>`);$('#detail-content').innerHTML=`<article class="panel search-card"><p><b>Hash</b> <code>${esc(data.hash)}</code></p><p><b>Producer</b> ${esc(data.producer)}</p><p><b>Transactions</b> ${num(data.tx_count)}</p></article>${table(['해시','From','To','Value'],rows)}`;}return true}
async function render(){try{if(await entity())return;const data=await fetch('/api/snapshot').then(r=>r.json()),kind=location.pathname.slice(1),c=data.chain||{};let rows=[];if(kind==='nodes'){$('#detail-title').innerHTML='메인 노드 <span>상세</span>';rows=data.nodes.map(n=>`<tr><td>${esc(n.name)}</td><td>${n.online?'ONLINE':'OFFLINE'}</td><td>${num(n.status?.height)}</td><td>${num(n.status?.peers)}</td><td>${num(n.latencyMs)} ms</td><td>${short(n.status?.blockHash)}</td></tr>`);$('#detail-content').innerHTML=table(['노드','상태','높이','피어','RPC','블록 해시'],paginate(rows));}else if(kind==='validators'){$('#detail-title').innerHTML='검증자 <span>상세</span>';rows=(c.validators?.validators||[]).map(v=>`<tr><td>${short(v.id)}</td><td>${Number(v.signingRatePercent).toFixed(2)}%</td><td>${num(v.signedBlocks)} / ${num(v.eligibleBlocks)}</td><td>${num(c.production?.producerBlocks?.[v.id]||0)}</td><td>${num(v.votingPower)}</td></tr>`);$('#detail-content').innerHTML=table(['검증자','서명률','서명','생성','Power'],paginate(rows));}else if(kind==='accounts'){rows=(c.accounts?.accounts||[]).map(a=>`<tr><td>${short(a.address)}</td><td class="amount">${esc(a.balanceFormatted)} IEUM</td><td>${a.locked?'잠금':'유통'}</td></tr>`);$('#detail-content').innerHTML=table(['주소','잔액','상태'],paginate(rows));}else{location.href=`/#${kind==='transactions'?'flow':'explorer'}`;}}catch(error){$('#detail-title').innerHTML='조회 <span>실패</span>';$('#detail-content').innerHTML=`<div class="alerts"><p class="critical"><b>SEARCH</b>${esc(error.message)}</p></div>`;}}document.addEventListener('click',e=>{if(e.target.dataset?.page){page=Number(e.target.dataset.page);render()}});render();
